server:
  port: 8080
  servlet:
    context-path:

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/UDPT_Server"
    username: root
    password: udptserver04@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
